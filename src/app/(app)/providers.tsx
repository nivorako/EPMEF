"use client";

import React from "react";

// Client-side providers for the app.
//
// Add future client providers here (e.g. framer-motion AnimatePresence).

export default function Providers({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
